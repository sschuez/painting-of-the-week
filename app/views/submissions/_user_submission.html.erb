<%= turbo_frame_tag submission do %>
  
  <div class="text-center">

    <p class="mb-1"><strong><%= submission.display_title %></strong></p>

    <%= cl_image_tag photo_or_fallback(submission.file), class: "submission__user__img" %>

    <div class="flex-center">
      <%= link_to "Edit submission", 
                  edit_submission_path(submission), 
                  class: "my-btn my-btn--dark mb-2",
                  data: { turbo_frame: dom_id(submission)} %>
    </div>

    <small class="text-muted">If unchanged, this work will be published at the end of this week.</small>
  
  </div>

<% end %>